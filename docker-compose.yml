version: '3.8'

services:
  # MySQL Database Service
  mysql:
    image: mysql:8.0
    container_name: dezhou-mysql
    environment:
      MYSQL_ROOT_PASSWORD: aida87014999
      MYSQL_DATABASE: dezhou
      MYSQL_USER: dezhou_user
      MYSQL_PASSWORD: dezhou_pass
    ports:
      - "33306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./database.sql:/docker-entrypoint-initdb.d/database.sql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10
    networks:
      - dezhou-network

  # Dezhou Poker Backend Service
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dezhou-backend
    depends_on:
      mysql:
        condition: service_healthy
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/dezhou?useUnicode=true&characterEncoding=utf8&allowMultiQueries=true&serverTimezone=UTC
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: aida87014999
      SPRING_PROFILES_ACTIVE: production
    networks:
      - dezhou-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  # Dezhou Poker Frontend Service
  frontend:
    build:
      context: ./vue-client
      dockerfile: Dockerfile
    container_name: dezhou-frontend
    depends_on:
      backend:
        condition: service_healthy
    ports:
      - "80:80"
      - "443:443"
    environment:
      - VITE_API_BASE_URL=http://localhost:8080
    networks:
      - dezhou-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost"]
      interval: 30s
      timeout: 5s
      retries: 3

# Networks
networks:
  dezhou-network:
    driver: bridge

# Volumes
volumes:
  mysql_data:
    driver: local