server:
  port: 8080

spring:
  application:
    name: texasholder-poker-server
  main:
    allow-bean-definition-overriding: true

  datasource:
    url: jdbc:mysql://localhost:33306/texasholder?useUnicode=true&characterEncoding=utf8&allowMultiQueries=true&serverTimezone=UTC
    username: root
    password: aida87014999
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

jwt:
  secret: dezhou-secret-key-1234567890-abcdefghijklmnopqrstuvwxyz
  expiration: 86400000
  header: Authorization
  prefix: Bearer

logging:
  level:
    root: INFO
    com.archy.texasholder: DEBUG
    org.springframework.web: INFO
    org.hibernate: INFO
